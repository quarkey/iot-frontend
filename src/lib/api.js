const API_URL = 'http://localhost:6001/api'

export default {

    // getSensor: function(id) {
    //     console.log("[LIB] fetching data from api: " + id)
    //     axios.get(`${API_URL}/sensors/${id}`, )
    //         .then(response => {
    //             console.log(response.data)
    //             return response.data
    //         })
    //         .catch(error => {
    //             return (this.error = true)
    //         })
    // }
}